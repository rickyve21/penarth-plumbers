import Link from 'next/link';import {ArrowRight,CheckCircle2,Phone} from 'lucide-react';import {areas,services,site} from '@/lib/site';
export function Hero({eyebrow='Plumbing services in Penarth',title,copy}:{eyebrow?:string,title:string,copy:string}){return <section className="hero"><div className="container hero-grid"><div><div className="eyebrow">{eyebrow}</div><h1 className="h1">{title}</h1><p className="lead">{copy}</p><div className="flex flex-wrap gap-3 mt-7"><Link href="/contact" className="btn btn-primary">Request a quote <ArrowRight size={18}/></Link><Link href="/contact" className="btn btn-secondary"><Phone size={18}/> Call for plumbing help</Link></div><p className="text-sm muted mt-4">Availability and response times vary by independent provider.</p></div><aside className="hero-panel"><div className="eyebrow !text-sky-300">Start your enquiry</div><h2 className="text-2xl font-black mt-2">Tell us what has gone wrong</h2><ul className="checks"><li>Describe the plumbing issue</li><li>Share your Penarth-area postcode</li><li>Mark urgent enquiries clearly</li><li>Get connected with suitable local help</li></ul><Link href="/contact" className="btn bg-white text-sky-950 w-full">Send an enquiry</Link></aside></div></section>}
export function TrustBadges(){return <div className="trust"><div className="container trust-row"><span>Local Penarth focus</span><span>Fast enquiry handling</span><span>Emergency enquiries accepted</span><span>Clear, simple quotes</span></div></div>}
export function ServiceGrid(){return <div className="grid3">{services.map((s,i)=><Link href={'/'+s.slug} className="card group" key={s.slug}><div className="text-sky-700 font-black text-sm">0{i+1}</div><h3 className="text-xl font-black mt-3">{s.title}</h3><p className="muted">{s.short}</p><span className="inline-flex items-center gap-1 text-sky-700 font-bold mt-3">View service <ArrowRight size={16}/></span></Link>)}</div>}
export function FAQ({items}:{items:{q:string,a:string}[]}){return <div className="faq">{items.map(x=><details key={x.q}><summary>{x.q}</summary><p>{x.a}</p></details>)}</div>}
export function CTASection(){return <section className="section"><div className="container"><div className="cta md:flex items-center justify-between gap-8"><div><div className="eyebrow !text-sky-300">Need plumbing help?</div><h2 className="h2">Start with a simple enquiry</h2><p className="text-slate-300">Tell us what you need and we’ll help connect you with suitable local plumbing services.</p></div><Link href="/contact" className="btn btn-primary shrink-0 mt-5 md:mt-0">Request a quote <ArrowRight size={18}/></Link></div></div></section>}
export function AreaList(){return <div className="areas">{areas.map(a=><span className="pill" key={a}>{a}</span>)}</div>}
export function FeatureList({items}:{items:string[]}){return <ul className="grid md:grid-cols-2 gap-3 list-none p-0">{items.map(i=><li className="flex gap-3 card" key={i}><CheckCircle2 className="text-sky-700 shrink-0"/><span>{i}</span></li>)}</ul>}
